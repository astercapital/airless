name: Validate packages

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

jobs:
  package_validate:
    strategy:
      matrix:
        package: [airless-core, airless-pdf]
    uses: ./.github/workflows/package-validate-reusable-workflow.yml
    with:
      package: ${{ matrix.package }}
